# Blog Automation Dependencies

# Core dependencies
httpx>=0.27.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
html2text>=2024.2.26
pydantic>=2.0.0
tenacity>=8.2.0

# Optional: Only needed if using Anthropic API instead of Claude Code CLI
# Uncomment if Claude Code CLI is not available in your environment
# anthropic>=0.40.0
